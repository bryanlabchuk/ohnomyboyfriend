extends Node
## Minimal boot scene - catches errors and keeps game running.
## If game crashes, we can switch main_scene to this to debug.

func _ready() -> void:
	# Ensure we don't exit immediately
	pass
